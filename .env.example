# Frontend (cliente): mantener en /api para usar proxy local o serverless
VITE_BACKEND_URL=/api

# Solo servidor (Vercel): URL real del backend (NO usar prefijo VITE_)
# Configurar en Variables de Entorno de Vercel
BACKEND_URL=https://oxlo-backend.onrender.com
